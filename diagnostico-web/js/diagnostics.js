// Registro central de diagnósticos.
// Cada diagnóstico puede vivir en su propio archivo JS y agregarse con:
// window.DIAGNOSTICS.push({ id, name, title, description, duration, courses, questions });
window.DIAGNOSTICS = window.DIAGNOSTICS || [];
